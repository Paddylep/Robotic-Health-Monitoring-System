This folder has the accelerometer code for the Arduino IDE needed to collect the data. It also had the CSV logging file. It is currently set up for a Windows system. If using a Linux or Apple system, you will need to modify the code slightly so it connects to your port.
You will need the Seeed_Arduino_LSM6DS3 library for the Arduino IDE when using the IMU.
