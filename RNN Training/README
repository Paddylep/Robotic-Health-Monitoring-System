This Folder contains the RNN training code; it was written in Google Colab. 
The program contains the code for taking in the training data, splitting it into the training, validation and test sets
training the model and exporting its weights in C++ format.
